apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Wave 2 — Dex identity provider wired to oauth2-proxy.
# NOTE: ships with default static user user@example.com / 12341234 — change before any real exposure.
resources:
  - https://github.com/kubeflow/community-distribution//common/dex/overlays/oauth2-proxy?ref=26.03.1
